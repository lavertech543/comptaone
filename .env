# --- Base de données ---
DB_NAME=nk_avicole
DB_HOST=127.0.0.1
DB_USER=nk_user
DB_PASSWORD=nk_123
DB_PORT=5432

# --- Sécurité ---
JWT_SECRET=change_this_jwt_secret_in_production
JWT_EXPIRES=8h
INACTIVITY_TIMEOUT_MIN=30

# --- E-mail (notifications) — optionnel ---
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
MAIL_FROM=no-reply@nk-avicole.local
